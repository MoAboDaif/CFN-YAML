name: Pull Request workflow 1

on:
  - pull_request

jobs:
  Cloudformation-linter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
#      - name: linter  install
#        uses: scottbrenner/cfn-lint-action@v2
#        with:
#          command:  cfn-lint -t ./vpc-template.yaml
          